-- =============================================================================
-- V022: shop_info 加「特色标签」字段，对应原型 ④ 店铺详情卡的 feature-chips
--   line 2572-2577：🔥招牌烤地瓜 / 炭火现烤 / 现做现卖 / 不赶时间
--
-- 存储：CSV 字符串，单标签 ≤ 8 字，最多 6 个，总长度 ≤ 64
-- 商户在 me/shop-edit 自填；shop-home 渲染 v-if 非空。
-- =============================================================================

ALTER TABLE `shop_info`
  ADD COLUMN `feature_tags` VARCHAR(255) DEFAULT NULL
  COMMENT '店铺特色标签 CSV，如 炭火现烤,现做现卖,不赶时间' AFTER `notice`;
