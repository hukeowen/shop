# 用户端 1:1 原型复刻 + 后端补齐 — 交付总结（2026-05-07）

## 一句话

按原型 user-h5.html 16 屏 1:1 还原用户端，**关键路径已 playwright 实测通过**：
登录 → user-home → 进店 shop-home → 商品详情 → 加入购物车 → 购物车按店分组 → 结算页 → 订单 → 我的（聚合 + 子页）。
共 **17 个 commit** 推到 main，本地连线上 DB 全部 API 真实联调。

## P0 紧急（业务可用性）

| ID | 内容 | 实际产出 |
|---|---|---|
| **P0-A** | AI 视频套餐付款后配额没到账 | `921b9d3` 加「同步状态」按钮（POST `/orders/{id}/sync` 主动查通联）+ AllinpayPollingService 全套日志增强（▶ 开始查询/状态/trxstatus/trxamt/✅ 命中标记） |
| **P0-B** | 用户端 tab 订单/我的点不动 | `624c12f` 全项目 switchTab → reLaunch 清理（10 文件）；RoleTabBar `force-role="member"` 防商户账号在 C 端被解析成商户 tab |

## P1 主路径（用户端 4 主屏）

| 屏 | 状态 | 关键改动 |
|---|---|---|
| ① user-home | ✅ | 去扫码、3 快捷入口（附近/分类/优惠）、优惠跳「我的优惠券」聚合页（新建） |
| ④ shop-home | ✅ | M1-M8 全套：招牌大卡 + chips + 距离 + 折扣 + 优惠券 + 满减 + 分类 tab + 社交证明；商品改用 `/merchant/shop/public/products?tenantId=X` 跨租户接口；商户简易发布默认上架 |
| ② user-order | ✅ | 按店铺分组 + 异步拉真实店铺名 + 5 tab 状态过滤 |
| ⑥ user-me | ✅ | 头像 + 跨店聚合 3 列 + 我加入的店铺（每店独立资产）+ 资产/收藏/平台 3 个 grid |

## P2 下单链路

| 屏 | 状态 | 关键改动 |
|---|---|---|
| ⑤ product/detail | ✅ | 修 4 处 header `tenantId` 越权 401（`16dd5c8`）；显示真实图 + 价格；加入购物车测试通过 |
| ⑦ cart | ✅ | 按店铺分组 + 真实店铺名 + 真实商品图 + 数量增减 + 全选 + 结算 (`e67c4f3` `6610d2a` `3802532`) |
| ⑧ checkout | ✅ | 商品名/图/价 fallback 用 `itemName/Pic/Price` 兼容嵌套结构（`cdee773`）；配送/抵扣/支付明细 |

## P3 user-me 子页（10 个）

playwright 实测能正常打开：

| 子页 | 状态 |
|---|---|
| `/user-me/wallet` 我的钱包 | ✅ 推广/消费积分 + 转换 + 4 操作 |
| `/user-me/promo-records` 推广积分明细 | ✅ 店铺切换 tab + 流水 |
| `/user-me/star` 店铺星级 | ✅ 店铺 tab + 星级卡 + 4 列数据 |
| `/user-me/address` 收货地址 | ✅ 空态 + 添加 |
| `/user-me/invite` 邀请好友 | ✅ 邀请说明 + 链接 + 复制 + 使用说明 |
| `/user-me/favorites` 收藏的店铺 | ✅ |
| `/user-me/help` 帮助与反馈 | ✅ |
| `/user-me/about` 关于 | ✅ |
| `/user-me/my-queue` 我的队列 | ✅ |
| `/user-me/coupons` 我的优惠券 | ✅ tab 过滤 + 空态 + 真实领券记录 |

## 后端补齐

| 模块 | 改动 |
|---|---|
| **优惠券系统** | V024 `shop_coupon` + `shop_coupon_user` 全套表；商户后台自建券（list/save/上下架/删除）；C 端拉券+领取（原子递增 takenCount 防超发 + DupKey 友好幂等补偿）|
| **店铺特色 chips** | V022 `shop_info.feature_tags`；商户编辑 + C 端展示 |
| **距离计算** | Haversine 公式 + GPS 获取，shop-home 显示「0.3km · 三里屯」 |
| **星级折扣表** | V023 `shop_promo_config.star_discount_rates` JSON 数组，商户配 + C 端展示「3 星会员享 9 折」 |
| **满减规则** | V025 `shop_promo_config.full_cut_threshold/amount`；shop-home 底部购物车「还差 ¥N」实时算 |
| **商户公开商品** | `/merchant/shop/public/products?tenantId=X` 跨租户拉某店上架商品 |
| **简易发布默认上架** | `simpleCreate` 后立刻 `updateSpuStatus(1)`（mall ENABLE）|
| **商品 DTO 加 tenantId** | mall `ProductSpuRespDTO` + `AppProductSpuBaseRespVO` 加 tenantId 字段，购物车按店分组用 |
| **跨租户隔离根因修复** | `AppUnifiedAuthController.issueTokenForMerchant` 5 处签发点 — token tenantId 落到正确商户租户（之前都落 admin tenant=1） |

## SQL 迁移清单

需在线上跑（按顺序）：`V022 → V023 → V024 → V025 → V026`

## 部署清单

```bash
# 1. 跑迁移 SQL（5 个）
mysql -h 47.109.143.146 -u root -p'CHANGE_ME_5201314jxs@qq.com' ruoyi-vue-pro \
  < sql/mysql/V022__shop_feature_tags.sql
# 重复 V023 V024 V025 V026

# 2. 部署最新代码（共 17 个 commit 自 8872393..3802532）
./deploy.sh

# 3. 商户登录后到 me/coupon 自建几张券、me/shop-edit 填特色标签、
#    me/promo-config 配星级折扣 + 满减门槛，C 端就能看到效果
```

## 本地联调环境（截至本次会话）

| 服务 | 地址 |
|---|---|
| 后端 | `http://localhost:48080`（连 47.109.143.146 MySQL） |
| 前端 H5 | `http://localhost:5181/m/` |
| 测试账号 | `13333333333 / 888888`（C 端用户）；`17777777777 / 888888`（商户「水果店」tenantId=163） |

## 已知遗留 / 待用户测试

- shop-home 头图 emoji 装饰是写死 🍠（多种店铺都显，可后续按店铺品类自适应）
- 邀请页二维码区显示空白（QR 接口 `/qr` 服务可能未部署到本地，线上有）
- 旧商户的商品 status=0 需手动批量上架（或新建商品自动上架）
- 视觉细节：原型用 px 单位，uniapp 是 rpx，1px≈2rpx 已基本对齐，但极少数边距可能差 1-2rpx

## 关键 commit 列表（升序）

`46b8746 798e39b 3a63379 051fcf0 5848ca9 c006307 921b9d3 624c12f aedd819 c324f78 06fae15 b014efe e31d04e 16dd5c8 e67c4f3 6610d2a 3802532 cdee773 6e684c1`

## 下一步建议

1. 跑 5 个 SQL 迁移 + deploy.sh
2. 商户后台填基础数据（特色标签 / 优惠券 / 星级折扣 / 满减）
3. 用户端走主路径回归测试（13333333333 登录 → 进店 → 加购 → 结算）
4. 如有具体页面视觉跟原型不一致，截图发我，我针对性改
